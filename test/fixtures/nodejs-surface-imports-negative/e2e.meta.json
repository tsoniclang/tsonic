{
  "expectFailure": true,
  "description": "nodejs surface rejects default imports and unsupported node modules.",
  "expectedErrors": [
    "TSN1004",
    "Default import is not supported for \"node:fs\"",
    "Unsupported module import: node:http"
  ]
}
