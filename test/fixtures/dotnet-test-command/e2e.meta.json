{
  "modes": ["dotnet"],
  "description": "tsonic test: generates an MSIL test project and runs dotnet test (xUnit).",
  "postBuild": {
    "commands": [
      "node ../../../packages/cli/dist/index.js test --project dotnet-test-command --config tsonic.workspace.json"
    ]
  }
}

