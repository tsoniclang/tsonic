/**
 * Function expression emitters (function expressions and arrow functions)
 * — facade re-exports.
 *
 * Implementations live in:
 *   - lambda-parameters.ts
 *   - lambda-body.ts
 */

export { emitFunctionExpression, emitArrowFunction } from "./lambda-body.js";
