// Simple anonymous object literal - auto-synthesized
export const point = { x: 10, y: 20 };

// Anonymous object with different property types
export const config = {
  name: "test",
  count: 42,
  enabled: true,
};

// Anonymous object with arrow function property
export const handler = {
  id: 1,
  process: (x: number): number => x * 2,
};

// Shorthand property syntax
const value = 100;
export const shorthand = { value };
