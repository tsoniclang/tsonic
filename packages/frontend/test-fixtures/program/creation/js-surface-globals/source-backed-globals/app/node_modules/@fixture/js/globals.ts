import type { JsValue } from "@tsonic/core/types.js";

declare global {
  interface String {
    trim(): string;
  }

  function setInterval(
    handler: (...args: JsValue[]) => void,
    timeout?: number,
    ...args: JsValue[]
  ): number;
}

export {};
