export interface DateTimeOffset$instance {
  AddMilliseconds(milliseconds: number): DateTimeOffset;
}

export const DateTimeOffset: {
  new(): DateTimeOffset;
};

export type DateTimeOffset = DateTimeOffset$instance;
