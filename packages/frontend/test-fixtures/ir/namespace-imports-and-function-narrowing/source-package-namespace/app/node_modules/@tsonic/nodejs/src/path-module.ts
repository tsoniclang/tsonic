export type ParsedPath = {
  readonly base: string;
};

export class ParsedPathError extends Error {}

export const basename = (value: string): string => value;
export const parse = (value: string): ParsedPath => ({ base: value });
