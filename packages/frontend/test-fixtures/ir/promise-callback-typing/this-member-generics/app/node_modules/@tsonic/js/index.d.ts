declare global {
  interface Array<T> {
    readonly length: int;
    slice(start?: int, end?: int): T[];
  }
  interface Map<K, V> {
    get(key: K): V | undefined;
  }
  interface MapConstructor {
    new <K, V>(): Map<K, V>;
  }
  const Map: MapConstructor;
}

export {};
