# Final Swing Execution Checklist — 2026-06-14

This checklist is the active low-level execution tracker for the final TSTS-backed Tsonic architecture branch.

Rules for this work:

```text
Do not stop at partial completion.
Do not treat a category as done until product code, tests, deletion gates, and downstream gates prove it.
Do not keep dual paths, legacy compatibility, or fallback TypeScript compiler API product paths.
Do not start final expensive gates until the code-completeness audit is signed off.
Report this checklist every 15 minutes while long-running work is active.
```

## Current Checkpoint

```text
branch: feature/tsts-final-completion
latest pushed commit before this checkpoint: e5793843 Remove raw program source fields
current local state: C# type rendering no longer accepts `undefined` as `object?`; required declaration, parameter, member, alias, lambda, dictionary, and JSON parse types report unsupported diagnostics when the lowering plan lacks a proven type; `Object.entries(...)` no longer invents `object` when the record value type is not proven; parameter and enum member lowering carries source diagnostics and the renderer reports missing names instead of inventing `arg`, `Member`, or `_`; public `compile()` accepts exactly one runtime entry and CLI generation errors when the emitted module set does not contain that exact entry; source-package `semanticMetadata` manifest overlays are removed so aliases/overloads cannot bypass TSTS source facts; core `JsValue` opacity is now a source-extension fact instead of a C# renderer name check; external binding target names are no longer carried as `sourceRuntimeName`; lowering carries `externalBinding: { bindingFile, sourceName }` and the C# emitter alone resolves `targetSurface[].targetName`; ambient `Array`/`JSON` validation uses TSTS symbol proof rather than raw callee text; `JsValue` JSON carrier recognition checks canonical core type nodes/declarations instead of accepting any type named `JsValue`; unbound `_`/private-looking names no longer produce opacity facts; array element, source primitive, union, intersection, alias-target, and unsupported-type renderer fallbacks now report diagnostics before using traversal placeholders; source/lowering declaration and member selection requires exact-one matches instead of first matching declaration/member
focused validation: @tsonic/frontend, @tsonic/csharp-emitter, and @tsonic/cli builds green after external-binding source identity split; @tsonic/frontend build green after exact-one declaration/member hardening; source-semantics + lowering focused suites 23 passing / 0 failing after exact-one declaration/member hardening; @tsonic/csharp-emitter build green after renderer missing-plan diagnostic hardening; @tsonic/csharp-emitter test package 12 passing / 0 failing after renderer missing-plan diagnostic hardening; @tsonic/frontend build green after deleting unbound opacity heuristics; focused source-semantics/lowering opacity subset 5 passing / 0 failing; @tsonic/frontend build green after ambient validation hardening; @tsonic/frontend focused Maximus validation files 64 passing / 0 failing after ambient validation hardening; @tsonic/csharp-emitter build green after removing simple-name and known-name target fallbacks; @tsonic/csharp-emitter test package 12 passing / 0 failing after removing simple-name and known-name target fallbacks; @tsonic/frontend build green after declaration-owner exact-one cleanup; @tsonic/frontend lowering plan builders subset 10 passing / 0 failing after declaration-owner exact-one cleanup; @tsonic/cli build green after declaration-owner exact-one cleanup; @tsonic/csharp-emitter build green after declaration-owner exact-one cleanup; @tsonic/frontend build green after exact-one fallback cleanup; @tsonic/frontend lowering plan builders subset 10 passing / 0 failing after exact-one declaration/signature cleanup; @tsonic/cli build green after exact-one fallback cleanup; @tsonic/csharp-emitter build green after exact-one fallback cleanup; @tsonic/frontend build green after TSTS module-graph runtime closure switch; @tsonic/frontend graph/source/program-creation subset 29 passing / 0 failing after TSTS module-graph runtime closure switch; @tsonic/cli build green after TSTS module-graph runtime closure switch; @tsonic/csharp-emitter build green after TSTS module-graph runtime closure switch; @tsonic/frontend build green after public graph-entrypoint removal and lowering graph fallback removal; @tsonic/cli build green after public graph-entrypoint removal and lowering graph fallback removal; @tsonic/csharp-emitter build green after public graph-entrypoint removal and lowering graph fallback removal; @tsonic/frontend focused graph/source/lowering subset 48 passing / 0 failing after `TsonicProgram.workspaceGraph` addition and fixture schema cleanup; @tsonic/frontend multi-character string overload regression 1 passing / 0 failing; @tsonic/cli NativeAOT library regression 1 passing / 0 failing after char-context fix; @tsonic/cli build green after deterministic package-folder resolution; @tsonic/cli restore/NuGet subset 14 passing / 0 failing after deterministic package-folder resolution; @tsonic/frontend lowering plan builders subset 9 passing / 0 failing after fallback removal; @tsonic/frontend dictionary subset 2 passing / 0 failing after removing dictionary fallback; @tsonic/cli build green after source-function selector deletion; @tsonic/frontend dictionary/runtime-visibility subset 4 passing / 0 failing; @tsonic/csharp-emitter record renderer subset 1 passing / 0 failing; previous @tsonic/frontend surface profile subset 14 passing / 0 failing; previous @tsonic/cli surface profile subset 15 passing / 0 failing; previous @tsonic/frontend lowering plan builders/source-semantics attribute subset 2 passing / 0 failing; previous @tsonic/csharp-emitter attribute renderer subset 1 passing / 0 failing; previous @tsonic/frontend validator/maximus suites 260 passing / 0 failing; previous targeted native-library/source-package CLI subset 8 passing / 0 failing
package validation: @tsonic/frontend full package test 418 passing / 0 failing; @tsonic/csharp-emitter full package test 3 passing / 0 failing
build validation: @tsonic/frontend, @tsonic/csharp-emitter, and @tsonic/cli build after current local changes; prior @tsonic/tsts build green after latest committed broad sweep
audit validation: product TSC import search clean outside vendored TSTS; frontend CLR/C#/System target leakage search clean across `packages/frontend/src`; frontend search for `targetSurface`, `targetName`, `System.*` through `sourceRuntimeName`, stale external-binding target helpers, and target package names is clean across `packages/frontend/src`; renamed focused frontend fixtures are clean for `@tsonic/dotnet`, `@fixture/dotnet`, `System.d.ts`, `System.js`, `CLR`, `clr`, `csharp`, and `dotnet`; old IR/source-text emission decision search clean except diagnostics/token labels; message-substring capability gating removed; product duplicate `Record` named-type checks removed outside source fact production; unused source-function signature fallback selector symbols removed from CLI; lowering fallback `unknown` search now reports only real source `unknown` and declaration-kind label; product search for `pickPackageFolder`, first package-folder indexing, source-signature first-choice fallback, and exact-nullish fallback is clean; product search for `buildModuleDependencyGraph`, `ModuleDependencyGraphResult`, and public `dependency-graph` paths is clean; product search for fallback `intrinsicTypePlan("object"|"unknown"|"any")` in frontend lowering/tsonic-extension is clean; product search for renderer/lowering invented `arg`/`Member` names is clean; product search for `semanticMetadata`, `overloadFamilies`, and manifest alias metadata is clean; product renderer search for `type.name === "JsValue"`, `["JsValue"`, and dotted type-name target inference is clean; product search for `sourceRuntimeName` compared with `System.*`/collections/threading is clean across frontend and C# emitter
full run-all: not restarted after current lowering sweep; final gates wait for code-completeness signoff
current incremental update: entry module and source-file queries now require exact-one matches instead of first-match selection; `compile()` no longer reads `filePaths[0]` before validating the exact one-entry contract; CLI generation rejects duplicate emitted entry modules; non-authoritative type-root namespace discovery no longer skips `_`/`internal*` directories by naming convention and instead relies on deterministic `index.d.ts` existence; malformed async `Promise`/async-union return plans now report unsupported diagnostics instead of inventing `void`/`object` awaited types; missing function and function-expression return type plans now report unsupported diagnostics instead of inventing `void`; named intersection alias targets now report unsupported diagnostics before emitting traversal placeholders; C# private JS runtime type rendering now uses full source-qualified identities instead of simple-name maps; stale CLI “TypeScript compilation failed” wording now says Tsonic source analysis; broad type-analysis ownership audit confirms product `typescript` API imports are clean and direct checker access is constrained to the TSTS source extension only; `this.value.join("|")` exposed a remaining receiver-shape dependency, so source runtime operation facts must now consume TSTS-resolved member declarations/signatures when the receiver expression still has declared union shape; focused validation green with @tsonic/frontend build, @tsonic/cli build, and @tsonic/csharp-emitter package tests `16 passing / 0 failing`
current TSTS ownership discovery: contextual generic calls exposed a stale source-signature storage preference, so call return storage must prefer the TSTS-instantiated use-site return type when the declaration signature still contains type parameters; non-null assertion over generic element access exposed type-only utility alias leakage, so erased wrapper lowering must use the wrapped/expected storage plan and type aliases must not receive runtime names unless a source binding fact proves runtime identity
current TSTS ownership cleanup: recursive local member lookup over aliases/unions/intersections/objects is deleted; property access receiver rendering now prefers the TSTS-resolved member owner for ordinary property access and TSTS flow/use-site receiver type for source-runtime receiver operations; focused native-library regression group is `15 passing / 0 failing`; focused destructured binding projection/source-fact coverage is `2 passing / 0 failing`
current TSTS ownership cleanup 2: destructuring binding projection is now produced by the TSTS source extension as `sourceBindingTypeProjectionFactKey`; lowering consumes that fact and no longer walks object/tuple/union lowering plans through `typeAtBindingAccess(...)`; focused destructured binding projection/source-fact regression is `2 passing / 0 failing`
current TSTS ownership discovery 3: whole-wrapper `getSymbolAtLocation(typeReferenceNode)` was a fragile stale pattern; source-semantics now resolves type-reference identity through the type-reference name node only, matching TSTS symbol ownership
current TSTS ownership cleanup 3: external binding target identity is split from frontend source identity; frontend only discovers nearest external `bindings.json` and emits `externalBinding` source references through `external-binding-source-identity.ts`, while the C# emitter owns `targetSurface` parsing and exact target classification such as `System.Threading.Tasks.Task` or `System.Collections.Generic.List\`1`; `sourceRuntimeName` no longer carries `System.*` target names; stale filename `external-binding-targets.ts` is removed
current TSTS ownership cleanup 4: unresolved external bindings no longer fall through to source/local names in the C# emitter; they report unsupported diagnostics at type/expression render sites. Frontend source tests and the focused package-resolution/package-root fixtures use neutral runtime/native vocabulary. The tsconfig declaration include matcher now treats `**/` as zero-or-more directories, so `types/**/*.d.ts` includes `types/augment.d.ts` like TypeScript. Focused validation after this cleanup: frontend source/package/lowering subset `33 passing / 0 failing`; C# emitter expression/module subset `13 passing / 0 failing`
current TSTS ownership cleanup 5: public `TsonicProgram.sourceFiles` and `TsonicProgram.declarationSourceFiles` are deleted; callers must use explicit program query functions for runtime, semantic, or declaration files. The stale `sourceRuntimeName` lowering/emitter contract is renamed to `sourceQualifiedName`, so source plans carry source identity only; target names remain target-side external-binding data inside the C# emitter. The stale underscore-name opacity test now asserts the greenfield rule: opaque runtime visibility is an explicit source fact such as canonical core `JsValue`, never a source-name heuristic. Focused validation after this cleanup: `@tsonic/frontend` build green; frontend source/program/source-semantics subset `46 passing / 0 failing`; `@tsonic/csharp-emitter` build green; C# emitter rendering/boundary subset `16 passing / 0 failing`
current TSTS ownership cleanup 6: lowering-side overload implementation selection is deleted. Overload family marker facts now attach only to TSTS-compatible overload declarations using the TSTS checker facade, and call sites receive `sourceOverloadCallImplementationFactKey` from the selected TSTS signature declaration. Lowering consumes that call-site fact and no longer re-scores overload implementations by locally comparing lowering type plans. Focused validation after this cleanup: `@tsonic/tsts` build green; `@tsonic/frontend` build green; frontend source-semantics/boundary subset `27 passing / 0 failing`; `@tsonic/csharp-emitter` build green after rerun without concurrent frontend clean; wide remnant sweep for old overload matcher symbols is clean outside the new boundary-test banned strings.
current TSTS ownership cleanup 7: the source semantic boundary test now permanently sweeps frontend source for target runtime identity terms, including stale source-runtime-name spellings, C#/CLR/dotnet terms, and `System.*` target names. Focused validation after this guard: frontend source-semantic-boundary subset `14 passing / 0 failing`; direct regex sweep over `packages/frontend/src` and `packages/frontend/dist` is clean for target runtime identity terms.
current TSTS ownership cleanup 8: stale frontend `runtimeNamed*` vocabulary is renamed to source-qualified vocabulary, and the target-identity boundary guard now bans that remnant spelling. Focused validation after this cleanup: frontend source-semantic-boundary subset `14 passing / 0 failing`; wide regex sweep over `packages/frontend/src` and `packages/frontend/dist` is clean for source-runtime-name, runtime-named, C#/CLR/dotnet, and `System.*` target identity terms.
current TSTS ownership cleanup 9: tuple element-access storage no longer falls back to the first tuple element. Lowering accepts a tuple storage element only for a statically proven numeric literal index and otherwise falls back to the TSTS use-site expression type. Focused validation after this cleanup: frontend source-boundary/destructuring/tuple subset `16 passing / 0 failing`; wide sweep for arbitrary tuple first-element storage is clean outside intentional test names.
current TSTS ownership cleanup 10: runtime-union carrier emission no longer picks the first arm for category matches such as array/string/function/object. Exact arm type still wins; category matches require exactly one matching arm; unresolved runtime-union carrier conversion reports unsupported instead of casting directly to the carrier. Focused validation after this cleanup: C# emitter package `18 passing / 0 failing`; sweep for runtime-union arm `findIndex`/tuple-branch `.find(...)` is clean.
current TSTS ownership cleanup 11: lowering no longer has a canonical `JsValue` source-name/package fallback for runtime opacity. `runtimeVisibility` is consumed only from `sourceRuntimeVisibilityFactKey`; the source extension remains the sole owner of canonical core `JsValue` recognition. Focused validation after this cleanup: frontend runtime-visibility/source-boundary subset `18 passing / 0 failing`; lowering search for the removed canonical fallback is clean.
current TSTS ownership cleanup 12: source runtime operation classification now prefers TSTS-resolved member declaration owners before receiver-shape classification for ordinary property access. This makes calls such as `this.value.join("|")` follow the checker-selected `@tsonic/js` member declaration instead of depending on the receiver's declared union shape. Focused validation after this cleanup: `@tsonic/frontend` build green; source-semantics subset `15 passing / 0 failing`; source-semantic-boundary subset `16 passing / 0 failing`; frontend target-identity/prewalk-remnant sweep is clean.
current TSTS ownership cleanup 13: the source semantic boundary now fails if old product analysis directories (`graph`, `ir`, or `symbol-table`) regain TypeScript files. Focused validation after this cleanup: source-semantic-boundary subset `17 passing / 0 failing`.
current TSTS ownership cleanup 14: byref passing modes now have focused C# render proof. Source facts remain `byref-writeonly-must-init`, `byref-readwrite`, and `byref-readonly`; the C# emitter renders them as `out`, `ref`, and `in` only at the target boundary. Focused validation after this cleanup: `@tsonic/csharp-emitter` build green; byref/runtime-union/record renderer subset `3 passing / 0 failing`.
current TSTS ownership cleanup 15: named runtime type rendering no longer has a silent `object?` fallback after a source-qualified/external-binding branch. External binding misses still report unsupported diagnostics inside `renderNamedRuntimeType`; source-qualified names render deterministically. Focused validation after this cleanup: `@tsonic/csharp-emitter` build green; external-binding/named-intersection/runtime-union/byref subset `4 passing / 0 failing`; renderer `?? "object?"`/`?? "void"` sweep is clean in type/expression/statement rendering.
current TSTS ownership cleanup 16: expression storage selection moved behind `sourceExpressionTypeProjectionFactKey`. The TSTS source extension now projects identifier, property-access, element-access, array-call, conditional, wrapper, and object-literal expression storage facts; lowering consumes those facts through `sourceExpressionProjectedStorageTypePlan` and the old lowering-side symbol storage cache/path walkers are deleted. Vendored TSTS helper `getTstsParameters()` / `getTstsTypeParameterNodes()` now returns `[]` for non-carrier nodes instead of leaking raw `Node.ParameterList` / `Node.TypeParameterList` panics. Focused validation after this cleanup: `@tsonic/tsts` build green; TSTS extension tests `6 passing / 0 failing`; `@tsonic/frontend` full package test `431 passing / 0 failing`; package-resolution/authoritative type-root focused suite `8 passing / 0 failing`; source semantics/lowering focused suite `28 passing / 0 failing`; legacy storage and target-runtime-identity remnant sweeps are clean.
current TSTS ownership cleanup 17: call argument types, call target types, declaration types, declaration return types, base-constructor parameters, contextual expression types, and self-referential initializer detection now move through source-extension facts (`sourceCallArgumentTypesFactKey`, `sourceDeclarationTypeProjectionFactKey`, `sourceExpressionTypeProjectionFactKey`, and `sourceInitializerReferencesDeclarationFactKey`). Lowering consumes these facts and no longer asks TSTS checker use-site questions for expression narrowing/context, call signatures, declaration return signatures, base constructor signatures, or initializer self-reference. The obsolete module-level `LoweringTypePlan` bucket is removed so type nodes are no longer accumulated as a parallel source IR inventory. Focused compile proof after this cleanup: `@tsonic/frontend`, `@tsonic/cli`, and `@tsonic/csharp-emitter` builds are green; product sweep for stale storage/source-runtime/CLR/TSC public-front-end remnants is clean.
current TSTS ownership cleanup 18: source-extension facts no longer use storage/lowering vocabulary. `field<T>` is represented as `FieldSemanticsFact { kind: "field" }`; lowering carries `fieldSemantics` and the C# emitter renders fields only at the target boundary. The source extension has no `storage`, `native-array`, `sourceExpressionStorageProjection`, `sourceRuntimeArrayCallProjection`, `sourceRuntimeObjectCallProjection`, conditional-type lowering helper, or non-nullish projection helper remnants. Empty stale frontend directories (`core-intrinsics`, `resolver-cases`, `symbol-table`) were removed, and the boundary test now fails on stale analysis directory existence even if the directory is empty. Focused validation after this cleanup: frontend source-boundary/extension/lowering subset `48 passing / 0 failing`; source-boundary subset `20 passing / 0 failing`; C# emitter package `19 passing / 0 failing`; `@tsonic/cli` build green; `git diff --check` clean; product remnant sweeps are clean for frontend target identity, frontend/CLI TSC API imports, checker APIs outside TSTS source-program boundary, bridge casts/non-null suppression, and old storage/helper symbols outside the boundary-test banned list.
current TSTS ownership cleanup 19: the dead `receiverOwnerTypePlan()` lowering branch is deleted, so property-access lowering has one receiver type source: `sourceExpressionTypeProjectionFactKey`. Redundant `sourceFile` parameters were removed from lowering source-text helpers after switching source text to the TSTS node API. `Function.length` no longer silently renders constant `0`; direct function-expression/arrow-function receivers compute formal length through the lowering plan, and every other `Function.length` receiver reports an unsupported diagnostic before emitting a traversal placeholder. Focused validation after this cleanup: frontend source-boundary/lowering subset `32 passing / 0 failing`; C# expression/boundary subset `13 passing / 0 failing`; sweeps are clean for the deleted receiver-owner helper path, extension storage/lowering leakage, bridge casts/non-null suppression, and `git diff --check`.
current TSTS ownership cleanup 20: lowering-side contextual expected-type propagation is deleted. `expressionPlan(...)` no longer accepts or threads an `expectedType`; object literal member context, array element context, spread context, return expression context, variable initializer context, equality-operand source primitive context, arrow-body context, and call-argument context are now attached by the TSTS source extension as `sourceExpressionTypeProjectionFactKey.contextualType`. Lowering consumes only that fact. Focused validation after this cleanup: frontend source-boundary/source-semantics/lowering/contextual subset `67 passing / 0 failing`; product remnant sweep for `expectedArgumentTypeForLiteralShape`, `objectMemberExpectedType`, `arrayReceiverArgumentExpectedTypes`, `binaryOperandExpectedType`, `expectedType`, and four-argument `expressionPlan(...)` is clean.
current TSTS ownership cleanup 21: C# external-binding override metadata no longer interprets `sourceQualifiedName` as a target metadata key. Override accessibility lookup now uses external target metadata only when the lowering plan carries an explicit `externalBinding` fact. A source-qualified generated type with the same textual name as a target binding no longer inherits target accessibility by name collision. Focused validation after this cleanup: C# external binding metadata subset `2 passing / 0 failing`; remnant sweep for `sourceQualifiedNameKey`/source-qualified name access inside `external-bindings.ts` is clean.
current TSTS ownership cleanup 22: lowering-side type-alias expansion no longer scans a source file by alias text and stops at the first matching `type` declaration. Alias targets now come from TSTS binding/projection facts only, so an imported type annotation such as `Box` cannot be redirected to a function-body `type Box = ...` by text collision. Focused validation after this cleanup: frontend recursive-alias/TSTS-alias-binding subset `2 passing / 0 failing`; remnant sweep for `localTypeAliasDeclaration` and the deleted source-file text scan is clean.
current TSTS ownership cleanup 23: alias recursion tracking no longer keys declarations by `sourceFile + aliasName`. The recursion guard now tracks the exact TSTS declaration node selected by TSTS binding/projection facts, so same-name declarations in different scopes cannot collide in lowering. Focused validation after this cleanup: frontend recursive-alias/TSTS-alias-binding subset `2 passing / 0 failing`; remnant sweep for `aliasKeys` and `sourceTypeAliasKey` is clean.
current TSTS ownership cleanup 24: callable interface target projection moved out of lowering. Lowering no longer scans interface members for a single `CallSignature` or constructs function type plans from interface member AST; the TSTS source extension projects callable-interface targets from the checker-selected call signature. Focused validation after this cleanup: frontend callable-interface/alias subset `4 passing / 0 failing`; remnant sweep for `singleInterfaceCallSignature` and `interfaceCallSignatureTypePlan` is clean.
current TSTS ownership cleanup 25: lowering-side alias expansion fallbacks are deleted. `sourceTypePlan(...)` no longer re-expands alias declarations from AST when a `sourceTypeNodeProjectionFactKey` is absent, and type-alias declarations no longer fall back to locally rebuilding alias targets from syntax. Alias target information now enters lowering only through TSTS source-extension projection facts. Focused validation after this cleanup: frontend callable-interface/alias/source-primitive subset `5 passing / 0 failing`; remnant sweep for `SourceTypePlanState`, `sourceTypeAliasTargetPlan`, `sourceTypeAliasDeclarationTargetPlan`, `sourceTypeArgumentPlans`, `aliasTargets`, and `aliasDeclarations` is clean.
current TSTS ownership cleanup 26: call-return source facts no longer expose raw return type AST nodes to lowering. `SourceCallArgumentTypesFact.returnTypeNode` and the lowering-side `callSignatureReturnStorageTypePlan(...)` bridge are deleted; projected array storage is derived in the single source-projection-to-lowering conversion path from the projected type's own source node. Focused validation after this cleanup: `@tsonic/frontend` build green; frontend source-boundary/source-semantics/lowering subset `24 passing / 0 failing`; remnant sweep for `returnTypeNode` and `callSignatureReturnStorageTypePlan` is clean.
current TSTS ownership cleanup 27: lowering-side expression type classification is deleted. `expressionSourceTypePlan(...)` and `declarationSourceTypePlan(...)` no longer infer `.length`, `Error.message`, explicit declaration types, or initializer source types from runtime-operation facts; expression type plans now consume `sourceExpressionTypeProjectionFactKey` only, and declaration/variable storage uses source declaration/type projection facts. Focused validation after this cleanup: frontend source-boundary/source-semantics/lowering subset `26 passing / 0 failing`; remnant sweep for `expressionSourceTypePlan`, `declarationSourceTypePlan`, and lowering-side `sourceOperation.owner/member` checks is clean.
current TSTS ownership cleanup 28: frontend array plans no longer carry backend storage vocabulary. `LoweringTypeRefPlan` arrays now carry `sourceOrigin: "external-binding"` instead of `storage: "native-array"`; the C# emitter owns the target decision through `isExternalBindingArrayType(...)` and renders those arrays as C# native arrays at the target boundary. A boundary test now bans `native-array`, `readonly storage?:`, and direct `.storage` comparisons from frontend source. Focused validation after this cleanup: frontend source-boundary/lowering subset `25 passing / 0 failing`; C# emitter array/external-binding subset `3 passing / 0 failing`; `@tsonic/frontend`, `@tsonic/csharp-emitter`, and `@tsonic/cli` builds green; product sweep for frontend/emitter stale `native-array` storage markers is clean.
current TSTS ownership cleanup 29: binding-pattern, object-literal, and type-literal property projection no longer invents synthetic names such as `item0` and no longer uses raw node text as a property-key fallback. The source extension derives property keys from TSTS AST identifiers and literal/computed-literal property names only; dynamic computed keys preserve absence for diagnostics instead of guessing. Focused validation after this cleanup: frontend source-boundary/destructuring/contextual subset `25 passing / 0 failing`; concrete proof: `{ 'value': literal, 0: zero, ['value']: computed }` over `{ value: int; 0: string }` produces source facts `literal: int`, `zero: string`, and `computed: int`, and `{ 0: "D", ["named"]: "E" }` over `{ 0: char; named: char }` gives both literals `char` contextual facts.
current TSTS ownership cleanup 30: static element-access projection no longer parses tuple indexes from raw node text. It reads the TSTS numeric-literal carrier text (`AsNumericLiteral(...).Text`) and accepts only unsigned decimal integer indexes for tuple projection; non-literal indexes preserve absence and fall back to the TSTS use-site expression projection. Focused validation after this cleanup: frontend source-boundary/expression/destructuring subset `25 passing / 0 failing`; remnant sweep for raw `indexExpression` text parsing is clean.
current TSTS ownership cleanup 31: source projection deduplication no longer uses source text as a type-node identity key. `SourceBindingProjectedType` keys now use TSTS node identity through a `WeakMap`, so same-spelled nodes in different scopes cannot collide and different spellings cannot become semantics. Focused validation after this cleanup: frontend source-boundary/expression/destructuring/flow subset `26 passing / 0 failing`; raw source-text sweep now reports only diagnostic snippets/location text and lowering source-label helpers.
current TSTS ownership cleanup 32: literal lowering no longer reads literal values from raw node text. String, no-substitution-template, numeric, and bigint literals use their TSTS literal carrier text, while raw node text remains only for diagnostic source labels. Focused validation after this cleanup: frontend literal/source-boundary/source-primitive/expression subset `74 passing / 0 failing`; raw source-text sweep now reports only diagnostic snippets/location text and lowering source-label helpers.
current TSTS ownership cleanup 33: the stale generic `SourceFrontend<T>` wrapper is deleted. Product and tests now construct the TSTS source program directly through `createTstsSourceProgram(...)`; there is no alternate source-engine facade or source-frontend abstraction to preserve a future dual path. Focused validation after this cleanup: frontend TSTS source-program/source-boundary/program-creation subset `30 passing / 0 failing`; product sweep for `SourceFrontend`, `createTstsSourceFrontend`, and `source-frontend-engine` is clean.
not done: final run-all, downstreams, branch hygiene, final PR report
```

## Low-Level Work Items

| ID | Area | Required final state | Current status | Remaining proof |
| --- | --- | --- | --- | --- |
| 0.1 | Baseline | Branch starts from current `main` | Done earlier | Reconfirm before PR |
| 0.2 | Hygiene | Branch hygiene clear | Pending | Run branch hygiene after final commit |
| 0.3 | Branch | One active branch for the final swing | Done | Continue on `feature/tsts-final-completion` |
| 0.4 | Spec | Canonical plan recorded | Done | Keep this checklist current |
| 0.5 | Baseline tests | Initial focused baseline known | Done earlier | Final gates still required |
| 1.1 | TSTS package contract | Tsonic consumes current TSTS public APIs | In progress | Audit private imports and vendored deltas |
| 1.2 | Extension host | Generic lifecycle is available | Done | TSTS extension host tests in final gate |
| 1.3 | Fact store | Typed sidecar facts are available | Done | Fact tests and no direct node mutation search |
| 1.4 | Checker facade | Types, symbols, signatures, narrowed/use-site types exposed | Expanded | Lowering tests prove use-site source primitives, contextual callables, and cross-module signature returns |
| 1.5 | Diagnostics | TSTS and extension diagnostics adapt through one path | Expanded | Capability suppression now uses typed extension diagnostic metadata, not message text; validator suites 260 passing |
| 1.6 | Import/module identity | Module graph/import identity usable from public TSTS API | In progress | Package/type-root tests green |
| 2.1 | SourceFrontend | Single frontend boundary exists | Done | Boundary test remains green |
| 2.2 | SourceProgram/SourceModule | TSTS-backed source program is the product model | Done | Final product audit |
| 2.3 | SourceDiagnostic | Diagnostics are frontend-neutral | Expanded | Whole-program and source-file validators use the same structured capability filter; validator suites 260 passing |
| 2.4 | CLI routing | CLI/build/test use frontend boundary | In progress | CLI tests and product import audit |
| 2.5 | No fallback frontend | No product TypeScript frontend remains | Clean in current product search | Final banned-search audit |
| 3.1 | Fact keys | Tsonic facts use typed keys | Done | Search for string-keyed fact lookup |
| 3.2 | Core imports | `@tsonic/core` imports resolve through extension identity | Done | Alias/shadow tests green |
| 3.3 | Numeric primitives | Numeric primitive facts attach in source terms | Done | Emitted-output fixture proof |
| 3.4 | Source package bindings | External bindings attach through canonical metadata | Expanded | Surface-profile target member semantics removed from manifests/resolvers; source package fixture/downstream proof still pending |
| 3.5 | Passing mode | `out`/`ref`/`inref` source facts are backend-neutral | Expanded | Source-extension fact tests and C# target-boundary render proof are green; full fixture/downstream proof pending |
| 3.6 | Attributes | Attribute slots are source facts, not CLR facts | Expanded | TSTS source fact, lowering plan, and C# renderer focused tests green; full attribute fixture proof pending |
| 3.7 | Native diagnostics | Tsonic source restrictions run through extension diagnostics | Expanded | Capability-dependent diagnostics carry `capabilityFeatureKey` metadata |
| 3.8 | Fact tests | Every fact family has positive/negative tests | Expanded | Expression/computed-name facts green; marker projection, binding projection, and C# struct/field/extension receiver tests green |
| 4.1 | TSTS builder | Program builder invokes TSTS | Done | Final product audit |
| 4.2 | Extension registration | Tsonic source extension is registered by default | Done | Creation tests green |
| 4.3 | Diagnostic adaptation | TSTS and extension diagnostics are converted once | Expanded | Message-based suppression deleted; validator suites 260 passing |
| 4.4 | Source files/modules | Runtime closure and semantic support files are separated | Expanded | Runtime source selection now traverses TSTS module graph from runtime seeds; focused Program Creation/source graph subset 29 passing / 0 failing |
| 4.5 | Facts exposure | Lowering consumes TSTS extension facts and module graph only | Done | Public checker and extension-host escape hatches removed; focused source-boundary tests green |
| 4.6 | Fixture comparisons | Key fixtures prove equivalent or intended behavior | Partial | Focused fixtures green |
| 5.1 | LoweringInput | Lowering reads TSTS source program, module graph, and fact store | Done | Direct checker sweep outside source extension is clean |
| 5.2 | Module/declaration plans | Declarations lower as AST/fact-backed plans | Expanded | TSTS marker and attribute facts now project into declaration/parameter plans; top-level structural helper collection covered by C# module renderer test |
| 5.3 | Type plans | Types render from source plans/facts | Expanded | Source primitive use-sites, alias targets, recursive alias guard, `Record<K, V>` dictionary plans, and no-fallback checker type lowering covered; full emitted fixtures still required |
| 5.4 | Expression/statement plans | Expressions/statements lower through plan builders | Expanded | New lowering and renderer tests green |
| 5.5 | Call plans | Calls use TSTS signatures and source-extension call facts, not local overload scoring | Expanded | `db.Events.map((e) => e.Path ?? "")` lowers to `List<string>` through the TSTS-instantiated call result; overload families now use a TSTS-selected call-site implementation fact; focused source-semantics/boundary subset `26 passing / 0 failing` |
| 5.6 | Member/index plans | Member/index access use checker answers and source facts | Expanded | Recursive local member lookup is deleted; `this.value.join("|")` and imported `cur.value` after `instanceof` lower through TSTS-resolved member owners/use-site receiver facts |
| 5.7 | Narrowing plans | Use-site type comes from TSTS source-extension facts | Expanded | TSTS checker use-site answers are projected only inside the source extension; erased wrappers no longer reify `NonNullable<T>` as runtime type; destructuring, flow, and contextual expression projections are now source-extension facts consumed by lowering |
| 5.8 | Synthetic declarations | Synthetic declarations are backend-neutral plan artifacts | Partial | Audit and fixture proof |
| 5.9 | Capability validation | Capability checks use source-feature terms | Expanded | Capability filtering is keyed by feature metadata; validator suites 260 passing |
| 6.1 | Module graph/diagnostics | TSTS owns semantic module graph | Expanded | Public old dependency-graph constructor deleted; lowering graph is built from the already-created TSTS program; runtime source selection and workspace edges now use TSTS module graph |
| 6.2 | Declarations/exports | Exports/declarations use TSTS graph/checker | Expanded | `struct`, `field<T>`, `Interface<T>`, and `thisarg<T>` are plan facts, not renderer source-name checks |
| 6.3 | Type refs/numerics | Type references use source facts | Expanded | Lowering tests prove `int`/`char`, recursive aliases, and ambient `Record<K, V>` dictionary facts; emitted fixture proof still required |
| 6.4 | Expressions/literals | Expression plans cover required source forms | Expanded | Char literals, array spread, and structural alias helper renderer tests green |
| 6.5 | Calls/overloads | Overload/generic resolution delegated to TSTS | Expanded | Generic call return storage now prefers TSTS-instantiated use-site types when source signatures contain type parameters; focused native-library group `15 passing / 0 failing` |
| 6.6 | Member/index access | Member/index lookup delegated to TSTS | Expanded | Local recursive member probing is deleted; Record value typing no longer uses named `Record` checks; Array/String/Map/Error/Function receiver operations attach from TSTS-resolved member declarations/signatures |
| 6.7 | Control-flow/narrowing | No eager branch narrowing engine remains | Expanded | Audit shows no product TSC imports, no emitter checker access, no `typeAtBindingAccess(...)`, no recursive local member storage lookup, and no lowering-side contextual expected-type propagation; remaining union hits are lowering-plan conversion or runtime union carrier rendering |
| 6.8 | Attributes | Attribute handling moved to source facts/lowering | Expanded | Descriptor/application facts, lowering plans, and C# renderer support implemented; full fixture proof pending |
| 6.9 | External source bindings | External binding facts use one manifest shape | Expanded | `memberSemantics` surface-manifest parser/merger removed from frontend and CLI; lowering now carries target-neutral `externalBinding` references; C# emitter resolves target names from binding metadata; source package fixture/downstream proof still pending |
| 6.10 | Synthetic declarations | Synthetic declarations no longer depend on old IR | Partial | Compile/audit proof |
| 7.1 | Delete `typescript` product imports | Product frontend does not import TSC | Clean in current product search | Final search gate |
| 7.2 | Delete old graph extraction | No product import/export semantic walkers as public entrypoints | Expanded | `buildModuleDependencyGraph`, `ModuleDependencyGraphResult`, and public `dependency-graph` path removed; TSTS module graph now decides post-build runtime source closure and workspace edges; remaining import-closure seeding is internal bootstrap for path mapping |
| 7.3 | Delete old symbol IDs | No target-rendered source IDs | Clean in current product search | Search for `symbolId`, `sourceId`, and target-rendered source identity terms is clean outside target-boundary tests |
| 7.4 | Delete old IR builder | No parallel source IR tree as product source of truth | In progress | `Record<K, V>` added as a lowering plan kind backed by source facts; remaining old-plan audit pending |
| 7.5 | Delete old inference/binding | No local checker/generic/call inference owner | Expanded | Standalone generic-function helper deleted; unused source-function signature fallback selector deleted; arbitrary first declaration/signature selection removed from lowering |
| 7.6 | Delete eager narrowing | No old narrowing engine in product path | Expanded | Lowering consumes TSTS extension facts only; checker/narrowed/contextual calls are confined to the source extension; recursive local member lookup, lowering-local destructuring projection, and lowering-side contextual propagation are deleted |
| 7.7 | Delete legacy manifests | One metadata schema only | Expanded | `AliasMetadataV1` removed; no V1/V2 bridge names remain in frontend/emitter product code |
| 7.8 | Delete backend leakage | No frontend CLR/C#/System facts | Expanded | Lowering no longer derives opacity from runtime/source names; external binding target names are no longer parsed in frontend; source extension attaches `sourceRuntimeVisibilityFactKey`; post-change frontend source sweep is clean for `targetSurface`, `targetName`, `System.*`, `CLR`/`Clr`, `csharp`, and `dotnet` |
| 8.1 | Frontend focused tests | Focused repaired suites green | Green for current lowering hardening | Latest focused run: source-boundary, deterministic typing, source package resolution, package roots, package resolution, plan builders, program input discovery, and surface profiles `33 passing / 0 failing` |
| 8.2 | Frontend full tests | Frontend package tests green | Green after metadata change | `418 passing / 0 failing` |
| 8.3 | TSTS package build | Vendored TSTS compiles | Green after metadata change | `npm run build --workspace @tsonic/tsts` |
| 8.4 | C# emitter tests | Plan renderer tests green | Green after async/intersection/missing-return renderer hardening | Latest focused expression/module/external/task/array run `13 passing / 0 failing`; prior package run `16 passing / 0 failing` |
| 8.5 | Full run-all | Complete Tsonic gate green | Pending | `./test/scripts/run-all.sh` |
| 8.6 | Downstreams | Proof pudding, tsumo, clickmeter, first-party consumers green | Pending | Run after run-all |
| 8.7 | Branch hygiene | No uncommitted/unmerged local work | Pending | Hygiene script and clean status |
| 8.8 | Final report | Commit range, changed files, deleted paths, tests, downstreams | Pending | Produce after gates |

## Active Failure Themes

| Theme | User-code example | Required fix |
| --- | --- | --- |
| Generic function value contextual use | `const id = <T>(x: T): T => x; const box: { run: (x: number) => number } = { run: id };` | Use TSTS contextual/checker answers; do not reject valid monomorphic contextual use |
| Generic function value escape | `const id = <T>(x: T): T => x; const obj = { id };` | Reject source-unsafe generic escape when no safe contextual callable target exists |
| Arrow contextual typing | `type Op = (a: number, b: number) => number; const ops: Op[] = [(a, b) => a + b];` | Recognize contextual type from the array/object container through TSTS facade |
| Type-root/source-package graph | `import type { PathLike } from "node:path";` | Use TSTS module/type-root graph; do not reconstruct a parallel resolver |
| Runtime closure filtering | `tests.entryPoint` imports only tests while semantic support files exist | Emit only runtime closure; keep semantic support for checking only |

## AST-Emission Cleanup Checkpoint

| Item | Before | Current state | Proof |
| --- | --- | --- | --- |
| Runtime expression semantics | Lowering recognized `console`, `Error`, `undefined`, and `.length` from local identifier text | TSTS source extension writes `expressionSemanticsFactKey`; lowering only consumes the fact | `source-semantics.test.js`: ambient/global positives and local-shadow negatives |
| Well-known computed names | Lowering recognized `[Symbol.iterator]` and `[Symbol.asyncIterator]` by `Node_Text` on receiver/member | TSTS source extension writes `wellKnownComputedNameFactKey`; declaration lowering consumes the fact | `source-semantics.test.js`: ambient/global positives and local-shadow negatives |
| Generic function aliases | C# module rendering used a module-wide alias map from source names | Source extension writes `genericFunctionAliasFactKey`; identifier plans carry `resolvedAliasName`; alias declarations carry `compileTimeOnly` | Focused generic-function tests green |
| Enum initializers | C# enum rendering used `initializer.literalText ?? "0"` | C# enum rendering calls `renderExpression(member.initializer, context)` | C# emitter build green |
| Marker declaration semantics | C# renderer filtered `extends struct` by checking `heritageType.name === "struct"` and had no plan fields for `field<T>` / `thisarg<T>` | TSTS source extension writes marker facts; lowering removes compile-time heritage markers and carries `sourceTypeKind`, `storageSemantics`, and `extensionReceiver` | Lowering marker projection test green; C# renderer struct/field/extension receiver tests green |
| Surface member semantics | Surface manifests carried target/runtime member behavior such as `storageAccess: "arrayLength"`, `emittedMemberName: "Length"`, and borrowed mutation write-back | Surface manifests now carry only package surface data: extension chain, type roots, and package requirements; runtime/member behavior must come from TSTS facts/lowering plans | Product search for `memberSemantics`, `emittedMemberName`, `storageAccess`, `borrowedMutationWriteBack`, `returnsArray`, `returnsReceiver`, and `mutatesReceiver` is clean |
| Runtime visibility | Lowering and source semantics marked named types opaque by checking runtime/source names or unbound names such as `_`, leading underscores, and U+FFFD | TSTS source extension attaches `sourceRuntimeVisibilityFactKey` only from resolved source binding facts; unbound names never produce opacity facts; lowering reads only that fact from use nodes, declarations, or resolved symbols | @tsonic/frontend build green; focused source-semantics/lowering opacity subset `5 passing / 0 failing`; product search for unbound opacity heuristics is clean |
| Dictionary type semantics | Lowering and the C# emitter treated `Record<K, V>` by checking named-type text such as `type.name === "Record"` | TSTS source extension attaches `sourceDictionaryTypeFactKey`; lowering emits a dedicated `record` type plan; C# rendering consumes the plan as dictionary storage; missing type arguments do not become `unknown` fallbacks | Focused frontend subset `2 passing / 0 failing` after no-fallback hardening; focused emitter subset `1 passing / 0 failing`; product search for named `Record` checks is clean |
| Source function signature selection | CLI package-manifest code had an unused selector that returned `exact ?? firstSignature` | Dead selector module deleted instead of retaining a fallback binding heuristic | Product search for `selectPreferredSourceFunctionSignature`, `SourceFunctionSignatureSurface`, and `source-function-surfaces` is clean; @tsonic/cli build green |
| Checker-derived unknown fallbacks | Lowering converted missing checker answers into broad `unknown` plans in recursive cycles, array elements, object literal members, and binding access | Lowering now preserves absence as absence; only real source `unknown` remains `intrinsic("unknown")` | @tsonic/frontend lowering plan builders subset `9 passing / 0 failing`; search for `intrinsicTypePlan("unknown")` now reports only source `unknown` |
| NuGet package-folder resolution | Restore planning selected the first `project.assets.json` package folder and projected compile DLL paths from that single folder | Restore planning now enumerates package folders deterministically, carries only compile DLL paths proven to exist, and preserves absence as absence for later diagnostics | @tsonic/cli build green; @tsonic/cli restore/NuGet subset `14 passing / 0 failing`; product search for `pickPackageFolder`, `folders[0]`, and `packageFolders[0]` is clean |
| Public graph entrypoints | Product callers could invoke `buildModuleDependencyGraph()` and old `dependency-graph` paths directly, creating a second program/lowering construction path | `compile()` now creates the TSTS program once and then builds the lowering graph from that program; public dependency-graph facades and lowering pipeline constructor exports are removed; lowering graph creation no longer accepts alternate options or re-resolves capabilities | @tsonic/frontend, @tsonic/cli, and @tsonic/csharp-emitter builds green; product search for `buildModuleDependencyGraph`, `ModuleDependencyGraphResult`, and `dependency-graph` is clean |
| Post-build module ownership | Frontend discovery pre-walked imports and then used that pre-walk as the runtime source list and workspace dependency edge list even after TSTS had built the source program | `program-assembly` now traverses `sourceProgram.moduleGraph` from runtime seeds to select runtime source files, and workspace dependency edges are emitted from resolved TSTS imports | @tsonic/frontend graph/source/program-creation subset `29 passing / 0 failing`; @tsonic/frontend, @tsonic/cli, and @tsonic/csharp-emitter builds green |
| Arbitrary first semantic selection | Lowering used `[0]`/`.find(...)` as fallback selectors for entry modules, runtime source-file checker anchors, base constructor signatures, symbol declarations, runtime type declarations, class/interface owners, and runtime visibility | Entry module mismatch is fatal; runtime checker anchor requires a runtime source file; construct signatures require exactly one candidate; declarations use value declaration or exact-one declaration; runtime visibility must be unique | @tsonic/frontend lowering plan builders subset `10 passing / 0 failing`; @tsonic/frontend, @tsonic/cli, and @tsonic/csharp-emitter builds green; product search for direct first declaration/signature/module fallbacks is clean |
| Declaration/member exactness | Source semantics and lowering selected the first matching declaration/member for source binding identity, attribute targets, type aliases, base constructors, class/interface members, object members, and binding paths | Source semantics and lowering now require exact-one matching declarations/members in those paths; ambiguous shapes preserve absence for diagnostics instead of selecting the first candidate | @tsonic/frontend build green; focused source-semantics + lowering suites `23 passing / 0 failing` |
| Char-compatible overload contextual typing | A call such as `Console.WriteLine("WRITE")` could inherit `char` expected type from an overload where `char` is source-modeled over string, causing C# char emission to reject a multi-character string | Lowering now strips char expected type from multi-character string-literal arguments while preserving it for one-character literals such as `Char.IsLetter("A")` | @tsonic/frontend multi-character string regression `1 passing / 0 failing`; @tsonic/cli NativeAOT library regression `1 passing / 0 failing` |
| Public checker anchor | `program.sourceChecker.getTypeAtLocation(node)` used one checker anchored to the first runtime source file | `TsonicProgram` no longer exposes `sourceChecker`, and `TstsSourceProgram` no longer exposes `withTypeChecker(...)`; semantic reads come through source facts or the module graph | @tsonic/frontend focused suite `51 passing / 0 failing`; product search for `sourceChecker`, `withTypeChecker`, and direct checker APIs outside the source extension is clean |
| Resolver context | `resolveImport()` accepted absent package/source context and could return an empty core declaration path sentinel | `resolveImport()` now requires project root, authoritative source-package roots, and declaration aliases; missing `@tsonic/core` declaration is a deterministic diagnostic | @tsonic/frontend build green; product search for `opts?`, optional authoritative maps, and empty resolved-path sentinel is clean |
| Core declaration package ownership | Core declaration setup used candidate-root probing, so an active but malformed `projectRoot/node_modules/@tsonic/core` could be treated as absent while another root satisfied `import type { int } from "@tsonic/core/types.js"` | Program discovery and import resolution now select one active core package root first; if that root exists but lacks `types.d.ts` or `lang.d.ts`, compilation reports that exact missing declaration instead of searching fallback roots | Focused resolver/discovery suite `14 passing / 0 failing`; concrete proof: a project-local `@tsonic/core/package.json` without `types.d.ts` reports `Active @tsonic/core package is missing required source declaration...` and does not add `@tsonic/core/types.js` path mapping |
| Local import missing-file diagnostics | `resolveLocalImport()` selected `existingCandidate ?? candidatePaths[0]` to report a missing local import | Missing local imports now report every deterministic candidate path and do not select an arbitrary non-existent candidate | @tsonic/frontend, @tsonic/cli, and @tsonic/csharp-emitter builds green |
| Source diagnostic scope | Source semantics extension treated missing `sourceDiagnosticFileNames` as a behavior switch | Source diagnostic scope is mandatory at TSTS program construction; test harness passes all runtime test source files; support declarations are not classified by omitted options | Source semantics `11 passing / 0 failing`; source frontend `3 passing / 0 failing` |
| Constructor runtime facts | `new Error(value)` attached `Error.constructor` to both the `NewExpression` and its child identifier | Constructor runtime operation is attached to the `NewExpression`; the identifier callee is not double-factored | Source semantics `11 passing / 0 failing` |
| Binding access over unions | Destructured binding type lookup selected the first non-nullish union arm and lived as lowering-local object/tuple/union path-walking | The TSTS source extension now emits `sourceBindingTypeProjectionFactKey` for destructured binding identifiers; lowering converts that fact to a type plan and falls back only to TSTS checker use-site type, with no local `typeAtBindingAccess(...)` path | Focused frontend/source-fact regression `2 passing / 0 failing`; concrete proof: `{ value: int } | { value: string }` lowers to `int32 | string`, not first-arm `int32` or erased `number | string` |
| Expression storage selection | Lowering owned a recursive symbol-storage cache plus local object/tuple/array/record path walkers for identifiers, property access, element access, array calls, and object literals | The TSTS source extension owns expression projection facts; lowering converts `sourceExpressionTypeProjectionFactKey` to type plans and no longer carries `symbolStorageTypes`, `sourceRuntimeExpressionStorageTypePlan`, `objectLiteralStorageTypePlan`, or `objectEntriesEntryArrayTypePlan` | `@tsonic/frontend` full package `431 passing / 0 failing`; source semantics/lowering focused suite `28 passing / 0 failing`; storage-remnant grep clean |
| Flow narrowing with source primitives | Identifier expression projection preferred the declared source-primitive shape before applying TSTS use-site narrowing, so `value: int \| string` inside `typeof value === "string"` could still project as `int \| string` | The source extension now refines declared source-primitive projections through the TSTS checker use-site projection; no lowering/emitter path recomputes narrowing | Source semantics full suite `18 passing / 0 failing`; focused source-boundary/expression subset `25 passing / 0 failing`; concrete proof: before branch `value` projects as `int \| string`, string branch projects as `string`, numeric branch projects as `int` |
| Call/declaration semantic selection | Lowering asked the checker for resolved call signatures, contextual expression types, declaration signatures, return types, base constructor construct signatures, and self-referential initializer symbols | The TSTS source extension now owns those selected semantic answers as facts; lowering maps fact projections into plans and the old module-level type-node bucket has been deleted | `@tsonic/frontend`, `@tsonic/cli`, and `@tsonic/csharp-emitter` builds green; no old storage/source-runtime/CLR/TSC public-front-end remnant hits |
| Runtime heritage selection | Base-constructor parameter lowering read `runtimeHeritageTypeNodes(...)[0]` | Runtime heritage is now accepted only when exactly one runtime heritage remains after compile-time marker erasure; no positional first heritage selection remains | Lowering focused subset `5 passing / 0 failing` |
| Source-front-end transpile path | `createTstsSourceFrontend()` exposed a TS-style `transpileModule()` API alongside Tsonic program analysis | Source frontend now only creates TSTS-backed source programs; product JS transpilation is not a Tsonic frontend path | Product search for `SourceTranspiler`, `SourceTranspile`, and frontend `transpileModule` is clean |
| Source-program switches | `createTstsSourceProgram()` accepted optional extensions, optional semantic checks, optional extension checks, and optional module-resolution paths | Tsonic source-program construction always registers the Tsonic extensions, always runs TSTS semantic and extension checks, and requires explicit module-resolution paths | Product search for optional extension/check toggles is clean except the intentional hardcoded enabled flags |
| Frontend scratch roots | Frontend test utilities wrote Tsonic-specific scratch trees under OS temp directories | Frontend test scratch roots are under repository `.temp/`, matching repo hygiene policy | Product/test search for frontend `os.tmpdir()` is clean; path-only `/tmp` containment test remains a pure string case |
| Type-root namespace discovery | Non-authoritative type-root discovery skipped directories whose names started with `_` or `internal` | Discovery now includes any namespace directory that proves an `index.d.ts`; no source/private semantics are inferred from directory names | @tsonic/frontend build green |
| Renderer missing-data defaults | C# expression rendering defaulted missing identifiers to `value`, missing members to `member`, missing number literals to `0`, missing callbacks to synthetic lambdas, missing arguments to `null`/empty strings, and missing array elements to `default!` | Renderer now reports unsupported source data for missing required AST/fact-backed plan fields; valid source-level defaults such as `join()` comma and `slice()` start `0` remain as JS semantics | C# emitter build green; expression renderer regression `4 passing / 0 failing`; product search for those renderer defaults is clean except JS default-argument semantics and C# definite-assignment split |
| Renderer broad type placeholders | Array literals, source primitives, unions, intersections, missing alias targets, malformed async return types, missing function/function-expression return types, and unsupported type plans could silently render broad `object?`/`void` placeholders | These renderer paths now report unsupported diagnostics before emitting traversal placeholders; valid opaque runtime carriers still map to `object?` by source fact | @tsonic/csharp-emitter build green; @tsonic/csharp-emitter test package `16 passing / 0 failing` |
| Binding-pattern variable labels | Variable declarations without identifier names fell back to synthetic `value` | Binding-pattern variable plans now carry the actual AST pattern text for diagnostics; no generated variable name is invented for patterns | @tsonic/frontend build green; product search for `nodeName(...) ?? "value"` is clean |
| Undefined type rendering | `renderCSharpType(undefined)` and `renderNullableCSharpType(undefined)` silently emitted `object?` | Type rendering now requires a concrete `LoweringTypeRefPlan`; optional call sites use `renderRequiredCSharpType`/`renderRequiredNullableCSharpType` and report unsupported diagnostics before continuing traversal | @tsonic/csharp-emitter build green; @tsonic/csharp-emitter test package `12 passing / 0 failing`; product search for optional-to-object render calls is clean |
| Generic special type arguments | Special named types such as `Array<T>`, `ReadonlyArray<T>`, `Iterable<T>`, and `Promise<T>` could flow through with missing type arguments | Special named type rendering requires type arguments and reports malformed plans; non-generic `Task`/void-return semantics remain explicit `void` handling | @tsonic/csharp-emitter test package `12 passing / 0 failing` |
| `Object.entries` value typing | `Object.entries(value)` with an unproven record value type became `Array<[string, object]>` | The lowering plan preserves absence as absence; the C# renderer reports the missing `Object.entries` value type instead of receiving a fabricated `object` element type | @tsonic/frontend build green; product search for `valueType ?? intrinsicTypePlan("object")` is clean |
| Parameter and enum member names | Missing parameter names lowered as `arg`, missing enum member names lowered as `Member`, and renderer sanitization could silently turn missing names into `_` | Parameter and enum member plans carry source kind/text; renderers report missing names before emitting a placeholder solely to continue traversal | @tsonic/frontend and @tsonic/csharp-emitter builds green; product search for invented `arg`/`Member` names is clean |
| Runtime entry module selection | `compile(filePaths, ...)` used `filePaths[0]`, and CLI generation used `emittedModules.find(entry) ?? entryModule` | `compile()` validates the one-entry contract before extracting the runtime entry; CLI generation requires exactly one emitted entry module and reports an invalid lowering/emission graph when it is missing or duplicated | @tsonic/frontend and @tsonic/cli builds green |
| Lowering graph entry selection | `createLoweringModuleGraph()` selected the first lowering module whose path matched the entry path | Entry module lookup now requires exactly one matching lowering module; duplicate entry modules are fatal instead of hidden by first-match selection | @tsonic/frontend build green |
| Source-package semantic overlays | `tsonic.package.json` could carry `semanticMetadata.aliases` and `semanticMetadata.overloadFamilies`, which were parsed into the normalized manifest even though no TSTS source fact proved them | Manifest semantic overlays are removed; aliases, recursion, and overload semantics must come from TSTS checker/source-extension facts over actual source declarations | @tsonic/cli build green; manifest-resolution focused test `8 passing / 0 failing`; product search for `semanticMetadata`, `overloadFamilies`, and manifest alias metadata is clean |
| `JsValue` opacity ownership | C# type rendering treated `type.name === "JsValue"` as opaque and mapped the source name directly to `object?` | The TSTS source extension attaches `sourceRuntimeVisibilityFactKey` to the canonical core `JsValue` declaration/use; the C# renderer consumes only `runtimeVisibility: "opaque"` | @tsonic/frontend and @tsonic/csharp-emitter builds green; source-semantics focused test `12 passing / 0 failing`; renderer search for `type.name === "JsValue"` and `["JsValue"` is clean |
| External binding target identity | Frontend lowering parsed `bindings.json.targetSurface[].targetName` into `sourceRuntimeName`, so source plans could carry C#/CLR names such as `System.Console`, `System.Threading.Tasks.Task`, or `System.Collections.Generic.List` | Frontend lowering now carries only `externalBinding: { bindingFile, sourceName }`; the C# emitter owns `targetSurface` parsing, target-name rendering, override accessibility, `Task`, and external collection classification; unresolved external bindings report unsupported diagnostics instead of falling through to source/local names | @tsonic/frontend, @tsonic/csharp-emitter, and @tsonic/cli builds green; frontend source search for `external-binding-targets`, `targetSurface`, `targetName`, and target identity terms is clean; sourceRuntimeName/System comparison search is clean across frontend and C# emitter |
| External binding source identity schema | The C# emitter could derive source binding names from target names, and checked-in binding fixtures still used missing `sourceName` or old `alias` fields | `targetSurface.types[].sourceName` is mandatory target metadata; the emitter maps exactly that source name plus its `$instance` companion and never derives source identity from target names | Frontend binding/metadata subset `13 passing / 0 failing`; C# emitter external/boundary/expression subset `14 passing / 0 failing`; checked-in binding schema stale count `0`; searches for `sourceNameForTargetName`, `value.alias`, target-name derivation, and `bindings.json` alias fields are clean |
| Source qualified identity naming | The lowering/emitter plan contract still used `sourceRuntimeName`, which confused source-qualified identity with target runtime identity and made broad sweeps harder to trust | The contract is now `sourceQualifiedName`; it is derived only from TSTS source binding facts/local source identity, while external target names are resolved solely by the C# emitter from `externalBinding` metadata | `rg 'sourceRuntimeName|SourceRuntimeName|resolvedClr|resolvedCLR|emittedClr|emittedCLR' packages/frontend packages/targets/csharp/emitter -g '!**/*.tsbuildinfo' -g '!**/node_modules/**'` is clean; frontend and C# emitter focused builds/tests are green |
| Program source-file access | `TsonicProgram` publicly exposed raw `sourceFiles` and `declarationSourceFiles`, inviting consumers to bypass the TSTS source-program/query boundary | Public raw fields are deleted; `runtimeSourceFiles` is private-to-query infrastructure and product callers use `getProgramRuntimeSourceFiles`, `getProgramDeclarationSourceFiles`, or `getProgramSemanticSourceFiles` | `@tsonic/frontend` build green; focused program/source tests `46 passing / 0 failing`; raw-field sweep reports only boundary-test banned strings and `program/queries.ts` |
| Ambient static validation | Source diagnostics for `Array`, `Array.isArray`, `JSON.parse`, and `JSON.stringify` used raw callee text in the validation path, so local shadows could inherit ambient restrictions | The validation path now uses the same ambient-global symbol proof as runtime fact production; `JsValue` JSON carrier checks resolve canonical core type nodes/declarations | @tsonic/frontend build green; focused Maximus validation files `64 passing / 0 failing`; product search for `isPropertyAccessNamed` and raw `Array`/`JSON` callee validation is clean |
| Type-reference symbol identity | Source semantics asked TSTS for symbols on whole `TypeReference` wrapper nodes in some paths | Source semantics now resolves type identity through the type-reference name node via one helper; this avoids wrapper-node symbol ambiguity and keeps binding identity aligned with TSTS checker ownership | @tsonic/frontend build green; destructuring projection focused tests `2 passing / 0 failing`; source-semantics search for direct wrapper `getSymbolAtLocation(node)` is clean except `symbolForName` |
| Attribute/overload selector identity | Attribute and overload builder selectors could recover a member target by local member-name text after symbol resolution failed | Selector lambdas now resolve the selected member through TSTS symbols and require the resolved declaration to belong to the intended owner declaration; overload family expansion uses the resolved symbol declaration set, not owner member text search | @tsonic/frontend source-semantics/boundary focused suite `36 passing / 0 failing`; product search for `memberDeclarationByName`, `memberDeclarationsByName`, and `selectedMemberName` is clean |
| Audit result | Legacy string/parser patterns existed in lowering/emitter | Targeted audit reports only diagnostic source snippets, diagnostic `sourceFile.Text()`, and AST token reads for names/literals | Audit command: `rg 'Node_Text\\(|\\.Text\\(|sourceText\\.(includes|startsWith|endsWith|match|split|slice|substring|replace)|literalText\\.(includes|startsWith|endsWith|match|split|slice|substring|replace)|nameSourceText\\.(includes|startsWith|endsWith|match|split|slice|substring|replace)|expressionRootName|buildGenericFunctionAliasMap|expressionAliases|LoweringExpressionAliasPlan|typeText|returnTypeText|declaredTypeText|contextualTypeText|operatorText' packages/frontend/src packages/targets/csharp/emitter/src -g '*.ts'` |
| Public checker/extension-host escape hatch | `TstsSourceProgram.withTypeChecker(...)` and the public `extensionHost` field let product/tests query or mutate raw extension/checker plumbing, preserving a second semantic path beside source facts | `TstsSourceProgram` exposes no checker callback and no raw extension host; consumers receive only `facts`, `moduleGraph`, source files, and diagnostics. Type examples read `sourceExpressionTypeProjectionFactKey`, and import examples read `moduleGraph.getImportBinding(...)` / `moduleGraph.getResolvedModule(...)` | @tsonic/frontend focused suite `51 passing / 0 failing`; product sweep for `sourceProgram.extensionHost`, `readonly extensionHost`, and direct checker APIs outside `tsonic-extension/source-semantics.ts` is clean |

Concrete source example now covered:

```ts
class Error {
  message: string;
  constructor(message: string) { this.message = message; }
}

export function local(value: string): void {
  const err = new Error(value);
  const size = value.length;
  void err;
  void size;
}
```

The local `Error` receives no `error-constructor` fact. The string `.length` receives a `length-property` fact because the TSTS checker proves the receiver type is `string`.

## Prior Lowering/Renderer Failure Themes

These were the blockers from the first full `run-all` attempt after frontend validation passed. The current sweep fixed the source-primitive/contextual-rendering subset and still requires full emitted fixture proof.

| Theme | User-code example | Previous bad C# shape | Current status |
| --- | --- | --- | --- |
| Explicit primitive aliases erased inside generic type nodes | `function createIntBox(value: int): Box<int> { return { value }; }` | `Box<number>` and `number` appears as a C# type | Lowering now preserves source primitive facts on declarations, identifiers, call returns, contextual literals, and alias targets; full fixture proof pending |
| Interface type members dropped | `interface Box<T> { value: T }` | `public interface Box<T> { }`, then `box.value` fails | Interface member lowering exists; full fixture proof pending |
| Intrinsics emitted as ordinary calls | `const x = defaultof<int>();` | `defaultof<int>()` remains in generated C# | Intrinsic fact rendering exists; full fixture proof pending |
| Destructuring names not declared | `const [first, second] = values;` | later generated code references `first`/`second` without declarations | Binding-pattern lowering exists; full fixture proof pending |
| Contextual callback params degrade to `object` | `values.map(x => x + 1)` under a typed delegate context | lambda parameter emits as `object x`, causing delegate conversion errors | TSTS contextual callable alias expansion covered by focused lowering tests; full fixture proof pending |
| Unsupported source expression nodes leak to renderer | `void f();`, `function* g(){ yield 1; }` | `KindVoidExpression` / `KindYieldExpression` unsupported in expression context | `void` and `yield` paths exist; final run-all must prove required fixture coverage |
| External source-package names lose qualified target identity | `new DefaultHttpContext()` from an external source package | bare `DefaultHttpContext` missing namespace/import in generated C# | Pending full source-package/downstream proof |

Concrete examples now covered by focused tests:

```ts
import type { char, int } from "@tsonic/core/types.js";

type Op = (value: int) => int;

export const ops: Op[] = [(value) => value];

export function read(letter: char): char[] {
  const same = letter === "A";
  return ["x", letter];
}
```

The lowering tree now carries `int32` into the contextual arrow parameter/return and carries `char` into both `"A"` and `"x"` string literals before the C# renderer runs.

## Reporting Cadence

Every 15-minute report during active long-running work must include:

```text
branch
latest commit
dirty/clean state
current checklist status deltas
focused pass/fail counts when tests are running
run-all/downstream status when started
CPU utilization row for long test runs
next concrete action
```
